pipeline {
  agent any 
  stages {
    stage ('git clone') {
      steps {
        git url: 'https://github.com/Bunnybalu/webapp-project.git' , branch: 'main'

      }
    }
    stage ('build') {
      steps {
        sh 'mvn clean install'

      }
    }
  }
}
